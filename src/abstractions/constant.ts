export interface IConstant {
  // Base interface contract for all constant classes
}

export abstract class BaseConstant implements IConstant {
  // Common helper or reflection methods for constants if required
}
